#!/bin/bash

ask simulate -t "open space apps ground control"    -l "en-US"  --force-new-session
ask simulate -t "change altitude to 5000"    -l "en-US"
ask simulate -t "navigate to alpha"         -l "en-US"